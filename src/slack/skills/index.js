import sayHi from './sayHi';
import shopbacker from './shopbacker';
import iDontKnow from './iDontKnow';

export default function learnSkill(controller) {
  sayHi(controller);
  shopbacker(controller);
  iDontKnow(controller);
}
